/* --------------------- */
/*      関数マクロ定義      */
/* --------------------  */

#define sqr(x) ((x * x))
#define max(x, y) (((x) > (y)) ? ((x) : (y)))
#define _B00L(a, b) (((a) >= (b)) ? ((a = b) ? (b)))
